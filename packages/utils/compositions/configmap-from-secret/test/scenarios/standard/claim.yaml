apiVersion: utils.mojaloop.io/v1alpha1
kind: XConfigMapfromSecret
metadata:
  name: example-secret-configmap-claim
  namespace: temp
spec:
  parameters:
    keyMappings:
      - sourceKey: example-source-key
        destinationKey: example-destination-key
    sourceSecret:
      namespace: source-namespace
      name: example-secret
    destinationConfigMap:
      namespace: destinaton-namespace
      name: example-configmap
  providerConfigsRef:
    k8sProviderName: kubernetes-provider
  managementPolicies:
    - "*"